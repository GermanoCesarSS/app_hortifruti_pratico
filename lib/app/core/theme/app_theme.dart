// coverage:ignore-file
import 'package:flutter/material.dart';

var _colorScheme = ColorScheme.fromSeed(seedColor: Colors.greenAccent);

final ThemeData themeData = ThemeData(
  colorScheme: _colorScheme,
  useMaterial3: true,
);
